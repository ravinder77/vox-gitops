plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1
filters:
  tag:Project: vox
hostnames:
  - private-ip-address
keyed_groups:
  - key: tags.Role
    prefix: role
groups:
  eks_nodes: "'eks-node' in (tags.Role | default(''))"
