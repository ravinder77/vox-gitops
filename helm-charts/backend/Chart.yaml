apiVersion: v2
name: vox-backend
description: Vox backend Node.js — deployed via Argo Rollouts canary
type: application
version: 0.1.0
appVersion: "1.0.0"

# Dependencies
dependencies: []
# If you add PostgreSQL or Redis as subcharts later, declare them here
